import { Query } from '../../../farel/directives/query'

export function main(FIREBASE_URL: string) {
  describe("Query", () => {
    it("should assign an expression result to a local variable within a template", () => {
      pending();
    });
  });
}
