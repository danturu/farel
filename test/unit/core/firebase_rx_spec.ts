import * as Firebase from 'firebase'

import { FirebaseRx } from '../../../farel/core/firebase_rx'

export function main(firebaseUrl: string) {
  describe("FirebaseRx", () => {
    it("should be tested", () => {
      pending();
    });
  });
}
