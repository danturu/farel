import * as Firebase from 'firebase'

import { ChildPipe } from '../../../src/queries/child_pipe'

export function main(firebaseUrl: string) {
  describe("ChildPipe", () => {
    it("should be tested", () => {
      pending();
    });
  });
}
