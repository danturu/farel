import * as Firebase from 'firebase'

import { LimitToFirstPipe } from '../../../firepipe/queries/limit_to_first_pipe'

export function main(firebaseUrl: string) {
  describe("LimitToFirstPipe", () => {
    it("should be tested", () => {
      pending();
    });
  });
}
