import * as Firebase from 'firebase'

import { EqualToPipe } from '../../../firepipes/queries/equal_to_pipe'

export function main(firebaseUrl: string) {
  describe("EqualToPipe", () => {
    it("should be tested", () => {
      pending();
    });
  });
}
