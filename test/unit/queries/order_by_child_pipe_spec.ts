import * as Firebase from 'firebase'

import { OrderByChildPipe } from '../../../firepipes/queries/order_by_child_pipe'

export function main(firebaseUrl: string) {
  describe("OrderByChildPipe", () => {
    it("should be tested", () => {
      pending();
    });
  });
}
