import * as Firebase from 'firebase'

import { OrderByValuePipe } from '../../../../farel/pipes/queries/order_by_value_pipe'

export function main(firebaseUrl: string) {
  describe("OrderByValuePipe", () => {
    it("should be tested", () => {
      pending();
    });
  });
}
