import * as Firebase from 'firebase'

import { ChildPipe } from '../../../../farel/pipes/queries/child_pipe'

export function main(firebaseUrl: string) {
  describe("ChildPipe", () => {
    it("should be tested", () => {
      pending();
    });
  });
}
