import * as Firebase from 'firebase'

import { LimitToLastPipe } from '../../../../farel/pipes/queries/limit_to_last_pipe'

export function main(firebaseUrl: string) {
  describe("LimitToLastPipe", () => {
    it("should be tested", () => {
      pending();
    });
  });
}
