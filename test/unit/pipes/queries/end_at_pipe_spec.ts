import * as Firebase from 'firebase'

import { EndAtPipe } from '../../../../farel/pipes/queries/end_at_pipe'

export function main(firebaseUrl: string) {
  describe("EndAtPipe", () => {
    it("should be tested", () => {
      pending();
    });
  });
}
