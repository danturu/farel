import * as Firebase from 'firebase'

import { ToObjectPipe } from '../../../../farel/pipes/terminals/to_object_pipe'

export function main(firebaseUrl: string) {
  describe("ToObjectPipe", () => {
    it("should be tested", () => {
      pending();
    });
  });
}
