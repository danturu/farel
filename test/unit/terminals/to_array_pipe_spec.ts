import * as Firebase from 'firebase'

import { ToArrayPipe } from '../../../firepipe/terminals/to_array_pipe'

export function main(firebaseUrl: string) {
  describe("ToArrayPipe", () => {
    it("should be tested", () => {
      pending();
    });
  });
}
