import * as Firebase from 'firebase'

import { NumChildrenPipe } from '../../../src/terminals/num_children_pipe'

export function main(firebaseUrl: string) {
  describe("NumChildrenPipe", () => {
    it("should be tested", () => {
      pending();
    });
  });
}
